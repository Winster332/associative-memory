<Window x:Class="VisionBrain.Windows.WindowWorkspace"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisionBrain.Windows"
		xmlns:ui="clr-namespace:VisionBrain.UI"
        mc:Ignorable="d"
		TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
		Style="{StaticResource MaterialWindowDark}"
        Title="WindowWorkspace" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
			<RowDefinition Height="70"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="25"/>
		</Grid.RowDefinitions>
		<ui:TopBar Grid.Row="0"/>
        <ui:LayoutView3D x:Name="View" Grid.Row="1"/>
		<ui:SmallInformPanel Grid.Row="2"/>
    </Grid>
</Window>
